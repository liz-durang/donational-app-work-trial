- if payment_method.new_record?
  p.notification.is-danger
    | You don't have any payment method on file. Please add your credit card using the form below.

  = render 'payment_methods/form'
- else
  .content data-accordion-panel-for='existing-card'
    p The following card will be used for your charitable contributions:

    .form.columns
      .column
        .field
          .label Name on card
          p.control.has-icons-left
            input.input disabled=true value=payment_method.name_on_card
            span.icon.is-left.is-small: i.fa.fa-user
      .column
        .field
          .label Card number
          p.control.has-icons-left
            input.input disabled=true value=masked_card_number(last4: payment_method.last4)
            span.icon.is-left.is-small: i.fa.fa-credit-card

    .field.has-text-right
      a.button.is-warning data-accordion-trigger='update-card' Use a different card

  .is-hidden data-accordion-panel-for='update-card'
    = render 'payment_methods/form'
    p.has-text-right
      a.button.is-text data-accordion-trigger='existing-card' Cancel
